<p id="notice"><%= notice %></p>

<h1>Дисциплины</h1>

<table>
  <thead>
    <tr>
      <th>Название</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @disciplines.each do |discipline| %>
      <tr>
        <td><%= discipline.title %></td>
        <td><%= link_to 'Темы', discipline_themes_path(discipline) %></td>
<!--         <td><%= link_to 'Show', discipline %></td>
        <td><%= link_to 'Edit', edit_discipline_path(discipline) %></td> -->
        <td><%= link_to 'Удалить', discipline, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Новая дисциплина', new_discipline_path %>
<br>
<%= link_to 'Главная страница', dashboard_index_path %>