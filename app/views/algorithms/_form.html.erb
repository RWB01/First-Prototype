<%= form_for @algorithm, url: theme_algorithms_path(@theme) do |f| %>
  <% if algorithm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(algorithm.errors.count, "error") %> prohibited this algorithm from being saved:</h2>

      <ul>
      <% algorithm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Имя алгоритма:" %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label "Описание алгоритма:" %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label "Файл с кодом алгоритма" %>
    <%= f.file_field :code %>
  </div>

  <div class="field">
    <%= f.text_field :theme_id, :type => "hidden" %>
  </div>

  <div class="actions">
    <%= f.submit "Сохранить алгоритм" %>
  </div>
<% end %>
