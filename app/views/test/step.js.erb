
  // $('.test_body').append("<%= @algorithm.title %>");
  // $('.test_body').append("<%= current_step.id %>");

$('.test_body').html("<%= escape_javascript render 'variables', :variables => current_step.variables %>");

$('.paramsHolder').attr('value', 'Next step');